<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/luncher_bg" >

    <TextView
        android:id="@+id/tv_splash_version"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="@string/version_number" />

    <ProgressBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@+id/tv_splash_version"
        android:layout_toLeftOf="@+id/tv_splash_version"
        android:maxHeight="24dip"
        android:maxWidth="24dip"
        android:minHeight="24dip"
        android:minWidth="24dip" />
    
    <TextView
        android:id="@+id/tv_spalsh_plan"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="40dp"
        android:layout_toLeftOf="@+id/tv_splash_versionname"
        android:text="TextView" 
        android:visibility="invisible"
        android:layout_marginLeft="20dp"
        />

</RelativeLayout>